# Руководство по использованию страницы "Учетные записи"

## 🎯 Описание

Страница "Учетные записи" предоставляет полный функционал управления учетными записями в системе Axenta CRM с современным интерфейсом в стиле Apple, автообновлением данных и расширенными возможностями фильтрации.

## 🚀 Функциональность

### ✅ Реализовано:

1. **API интеграция** - подключение к `https://axenta.cloud/api/cms/accounts/`
2. **Автообновление** - данные обновляются каждую минуту в фоновом режиме
3. **Таблица с данными** - отображение всех полей учетных записей с кастомными шаблонами
4. **Расширенная фильтрация** - по типу аккаунта, статусу, названию, родительскому аккаунту
5. **Множественный поиск** - поддержка поиска по нескольким компаниям через запятую
6. **Пагинация** - настраиваемое количество записей на странице (10-1000 или все)
7. **Статистика** - карточки с ключевыми показателями
8. **Действия с аккаунтами** - активация/деактивация, вход в системы, перемещение, удаление
9. **Детальные подсказки** - всплывающие окна с полной информацией
10. **Темная тема** - полная поддержка темной темы
11. **Responsive дизайн** - адаптация под мобильные устройства
12. **Кэширование** - оптимизация производительности с кэшем на 5 минут

### 📊 Отображаемые данные:

**Основная таблица:**
- **ID** - уникальный идентификатор с детальной подсказкой
- **Компания** - название аккаунта (`name`) с полной иерархией
- **Тип** - клиент или партнер с цветовой индикацией
- **Объекты** - количество активных/общих/удаленных с детальной подсказкой
- **Статус** - активен/заблокирован с визуальными индикаторами
- **Создан** - дата создания в кратком формате
- **Действия** - кнопки управления аккаунтом

**Статистические карточки:**
- **Всего записей** - общее количество учетных записей
- **Активных** - количество активных аккаунтов
- **Клиентов** - количество клиентских аккаунтов
- **Партнеров** - количество партнерских аккаунтов

**Детальные подсказки:**
- **ID подсказка** - полная информация об аккаунте, администраторе, иерархии
- **Объекты подсказка** - детальная статистика по объектам мониторинга

## 🔧 Настройка для тестирования

### 1. Установка токена авторизации

В консоли браузера выполните:

\`\`\`javascript
// Токен пользователя glomos
localStorage.setItem('axenta_token', '5e515a8f2874fc78f31c74af45260333f2c84c35');

// Данные пользователя
localStorage.setItem('axenta_user', JSON.stringify({
  id: "glomos-user",
  username: "glomos", 
  name: "GLOMOS",
  accountType: "partner",
  isActive: true,
  accountId: 2061
}));

// Данные компании
localStorage.setItem('axenta_company', JSON.stringify({
  id: "glomos-company-id",
  name: "GLOMOS",
  isActive: true
}));
\`\`\`

### 2. Доступ к странице

- **URL**: `http://localhost:5173/accounts`
- **Навигация**: Боковое меню → "Учетные записи"

## 🎮 Управление автообновлением

### Автоматическое обновление:
- **Интервал**: 60 секунд (1 минута)
- **Фоновый режим**: обновление происходит без прерывания работы пользователя
- **Умное обновление**: данные обновляются только при наличии изменений
- **Плавная анимация**: статистика обновляется с анимацией

### Индикаторы состояния:
- **Анимация загрузки** - при выполнении запроса
- **Плавные переходы** - при обновлении данных
- **Время последнего обновления** - отображается в консоли разработчика

## 📱 Возможности интерфейса

### 🔍 Расширенная фильтрация:
- **Поиск по названию** - в реальном времени с debounce (500ms)
- **Множественный поиск** - поддержка поиска по нескольким компаниям через запятую
- **Тип аккаунта** - клиент/партнер с визуальными индикаторами
- **Статус** - активные/заблокированные с цветовой кодировкой
- **Родительский аккаунт** - фильтрация по иерархии
- **Кнопка "Сбросить"** - очистка всех фильтров с анимацией
- **Счетчик активных фильтров** - отображение количества примененных фильтров

### 📊 Таблица данных:
- **Сортировка** - по всем колонкам (ID, Компания, Тип, Объекты, Статус, Дата создания)
- **Пагинация** - настраиваемое количество записей (10, 25, 50, 100, 200, 500, 1000, Все)
- **Компактный дизайн** - оптимизированное отображение данных
- **Детальные подсказки** - всплывающие окна с полной информацией
- **Responsive** - адаптация под размер экрана
- **Темная тема** - полная поддержка темного режима

### 🎯 Действия с аккаунтами:
- **Активация/Деактивация** - переключение статуса аккаунта
- **Вход в CMS** - доступ к системе управления (только для партнеров)
- **Вход в мониторинг** - доступ к системе мониторинга
- **Перемещение** - передача аккаунта другому партнеру
- **Удаление** - удаление аккаунта с подтверждением

### 📈 Статистика:
- **Всего записей** - общее количество учетных записей
- **Активных** - количество активных аккаунтов
- **Клиентов** - количество клиентских аккаунтов  
- **Партнеров** - количество партнерских аккаунтов
- **Анимированные карточки** - плавное обновление значений

## 🔍 Структура данных API

Каждый аккаунт содержит:

\`\`\`typescript
interface Account {
  id: number;                    // Уникальный ID
  name: string;                  // Название компании (отображается в столбце "Компания")
  type: "client" | "partner";   // Тип аккаунта
  adminFullname: string;        // ФИО администратора
  adminId: number;              // ID администратора
  adminIsActive: boolean;       // Активность администратора
  parentAccountName: string;    // Родительский аккаунт
  objectsActive: number;        // Активные объекты
  objectsTotal: number;         // Всего объектов
  objectsDeleted: number;       // Удаленные объекты
  comment: string | null;       // Комментарий
  isActive: boolean;            // Статус аккаунта
  blockingDatetime: string | null; // Дата блокировки
  hierarchy: string;            // Полная иерархия
  daysBeforeBlocking: number | null; // Дней до блокировки
  creationDatetime: string;     // Дата создания
}
\`\`\`

## 🛠 Техническая информация

### Файлы:
- **Сервис**: `src/services/accountsService.ts` - API интеграция и обработка данных
- **Компонент**: `src/views/AccountsPage.vue` - основной компонент страницы (3800+ строк)
- **Маршрут**: `src/router/index.ts` - настройка маршрутизации
- **Стили**: встроенные scoped стили с поддержкой темной темы

### Технологический стек:
- **Vue 3** - Composition API с `<script setup>`
- **Vuetify 3** - UI компоненты и темизация
- **Axios** - HTTP запросы с interceptors
- **Lodash-es** - debounce для поиска
- **Vue Router** - навигация и маршрутизация

### Архитектурные особенности:
- **Кэширование** - 5-минутный кэш для оптимизации производительности
- **Гибридная фильтрация** - серверная + клиентская фильтрация
- **Умное обновление** - сравнение данных перед обновлением UI
- **Плавные анимации** - анимированное обновление статистики
- **Responsive дизайн** - адаптация под мобильные устройства

### Обработка ошибок:
- **401 Unauthorized** - автоматическое перенаправление на логин
- **403 Forbidden** - уведомление о недостатке прав
- **404 Not Found** - уведомление о недоступности ресурса
- **Network errors** - отображение ошибки пользователю
- **Timeout** - 30 секунд на запрос
- **Snackbar уведомления** - пользовательские уведомления об ошибках

## 🎯 Следующие шаги

1. **Редактирование аккаунтов** - добавить возможность изменения данных через модальное окно
2. **Экспорт данных** - выгрузка в Excel/CSV с настройкой полей
3. **Расширенные уведомления** - push-уведомления о критических событиях
4. **Массовые операции** - выбор нескольких аккаунтов для групповых действий
5. **Права доступа** - ограничение по ролям пользователей и уровням доступа
6. **Аудит действий** - журнал изменений и действий пользователей
7. **Расширенная аналитика** - графики и диаграммы по статистике
8. **API оптимизация** - реализация GraphQL для более эффективных запросов

## 🔧 Детальное описание функций

### 🎨 Интерфейс в стиле Apple:
- **Минималистичный дизайн** - чистые линии и современная типографика
- **Карточки статистики** - AppleCard компоненты с градиентами
- **Плавные анимации** - переходы и hover-эффекты
- **Цветовая схема** - адаптивная под светлую и темную темы
- **Компактная пагинация** - элегантные элементы управления

### 🔍 Система фильтрации:
- **Умный поиск** - поиск по названию, администратору, родительскому аккаунту
- **Множественный поиск** - поддержка поиска по нескольким терминам через запятую
- **Чипы поиска** - визуальное отображение активных поисковых терминов
- **Гибридная фильтрация** - комбинация серверной и клиентской фильтрации
- **Кэширование фильтров** - оптимизация повторных запросов

### 📊 Детальные подсказки:
- **ID подсказка** - полная информация об аккаунте:
  - ID учетной записи и администратора
  - Тип компании и статус
  - ФИО администратора и комментарий
  - Полная иерархия и дата блокировки
  - Дни до блокировки и дата создания
- **Объекты подсказка** - статистика по объектам мониторинга:
  - Количество активных объектов
  - Общее количество объектов
  - Количество удаленных объектов

### ⚡ Производительность:
- **Кэширование данных** - 5-минутный кэш для часто запрашиваемых данных
- **Ленивая загрузка** - загрузка данных по мере необходимости
- **Debounce поиска** - оптимизация запросов при вводе
- **Умное обновление** - обновление только при изменении данных
- **Фоновое обновление** - автообновление без прерывания работы

## 🐛 Отладка

### Консоль браузера:
- **Логи запросов**: `🔍 Загрузка учетных записей с параметрами`
- **Логи ответов**: `✅ Получен ответ: { count: X, results: Y }`
- **Ошибки**: `❌ Ошибка загрузки учетных записей`
- **Фильтрация**: `🔧 Обнаружены активные фильтры`
- **Кэширование**: `🔧 Используем кэшированные данные`

### Проверка состояния:
\`\`\`javascript
// Проверка токена авторизации
console.log('Token:', localStorage.getItem('axenta_token'));
console.log('User:', localStorage.getItem('axenta_user'));
console.log('Company:', localStorage.getItem('axenta_company'));

// Проверка состояния компонента (в консоли Vue DevTools)
console.log('Accounts:', this.accounts);
console.log('Filters:', this.filters);
console.log('Stats:', this.stats);
\`\`\`

### Мониторинг производительности:
- **Network tab** - отслеживание API запросов
- **Performance tab** - анализ производительности рендеринга
- **Vue DevTools** - отладка состояния компонента
