# Где просматривать историю автоматических снимков

## 🎯 Расположение

История автоматических снимков доступна в **Настройках системы**.

### Путь для доступа:

```
Главное меню → ⚙️ Настройки → 📷 История снимков
```

---

## 📍 Пошаговая инструкция

### Шаг 1: Откройте Настройки

В главном меню нажмите на **"Настройки"** или перейдите по адресу:
```
http://localhost:3000/settings
```

### Шаг 2: Выберите вкладку "История снимков"

В левом боковом меню найдите и нажмите на:
```
📷 История снимков
   Автоматическое создание
```

Она находится между вкладками:
- ✅ **Мониторинг** (выше)
- ⚙️ **Компания** (ниже)

### Шаг 3: Просмотр информации

На странице вы увидите:

#### 📊 Статистика (вверху)
Четыре карточки с общей информацией:
- **Всего задач** - общее количество запусков
- **Успешно** - успешно завершенных задач
- **Ошибок** - задач с ошибками
- **Снимков создано** - общее количество снимков

#### 📋 Таблица задач
Список всех запусков с информацией:
- **ID** - номер задачи
- **Статус** - успешно/ошибка/частично/выполняется
- **Тип** - авто (ежедневно)/вручную/по расписанию
- **Начало** - дата и время запуска
- **Длительность** - сколько времени заняло выполнение
- **Статистика** - количество успехов/ошибок и договоров
- **👁️** - кнопка для детального просмотра

### Шаг 4: Просмотр деталей задачи

Нажмите на кнопку **👁️** (глаз) в любой строке таблицы.

Откроется диалоговое окно с подробной информацией:

#### Основная информация:
- Статус выполнения
- Тип задачи
- Дата и время начала/завершения
- Длительность выполнения
- Кто запустил задачу

#### Статистика:
- Количество компаний
- Количество договоров
- Успешно созданных снимков
- Количество ошибок

#### Ошибки (если есть):
- Полный список всех ошибок
- Время возникновения
- Номер договора
- Тип ошибки
- Сообщение об ошибке

---

## 🔄 Обновление данных

Нажмите кнопку **🔄** (обновить) в правом верхнем углу карточки для обновления списка задач.

---

## 🎨 Статусы задач

| Статус | Цвет | Описание |
|--------|------|----------|
| ✅ Успешно | Зеленый | Все снимки созданы без ошибок |
| ⚠️ Частично | Желтый | Есть успешные и неудачные попытки |
| ❌ Ошибка | Красный | Все попытки завершились с ошибками |
| 🔄 Выполняется | Синий | Задача в процессе выполнения |

---

## 📱 Альтернативные способы просмотра

### 1. Через API (для разработчиков)

```bash
# Последняя задача
curl http://localhost:8080/api/auth/snapshot-jobs/latest \
  -H "Authorization: Token YOUR_TOKEN" \
  -H "X-Tenant-ID: 1"

# Статистика
curl http://localhost:8080/api/auth/snapshot-jobs/stats \
  -H "Authorization: Token YOUR_TOKEN" \
  -H "X-Tenant-ID: 1"
```

### 2. Напрямую в базе данных

```sql
-- Последние 10 задач
SELECT 
  id,
  job_type,
  status,
  started_at,
  duration_seconds,
  success_count,
  error_count
FROM snapshot_jobs
ORDER BY started_at DESC
LIMIT 10;
```

---

## ❓ Частые вопросы

### Когда создаются снимки?
Автоматически каждый день в **00:00 UTC** (03:00 по МСК).

### Как часто обновляется история?
После каждого запуска планировщика автоматически создается новая запись.

### Сколько хранится история?
По умолчанию все записи сохраняются. Рекомендуется периодически очищать записи старше 90 дней.

### Можно ли удалить старые записи?
Да, через API endpoint:
```
DELETE /api/auth/snapshot-jobs/cleanup?days=90
```

### Что делать если задача зависла в статусе "Выполняется"?
Проверьте логи сервера. Если процесс был прерван, обратитесь к администратору для ручного обновления статуса.

---

## 🔗 См. также

- [Документация по логированию снимков](../../backend_axenta/docs/snapshot_jobs_logging.md)
- [API endpoints для работы с историей](../../backend_axenta/docs/snapshot_jobs_logging.md#api-endpoints)

